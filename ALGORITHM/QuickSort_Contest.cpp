#include <bits/stdc++.h>
using namespace std;

int main() {
  /*
Have to make a variable that can hold int & char at a time,
then have to partition it and apply quick sort to it.
also check if it is stable or not(if the element given at first comes to first
in the sorted vertion also than it is stable) out put it
*/

  return 0;
}